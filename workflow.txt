[START]
  |
  +---> [1] INITIAL SETUP PATTERN (Runs Once)
  |       |
  |       +---> 1.1: Import 'random' library
  |       +---> 1.2: Register helper functions into memory
  |       +---> 1.3: Print welcome banner
  |       +---> 1.4: Capture & clean player name via input().strip()
  |
  +---> [2] MATCH CONFIGURATION PATTERN (Outer Loop - Resets on New Match)
          |
          +---> 2.1: Prompt user for Game Mode selection (1, 2, or 3)
          |
          +---> 2.2: Evaluate mode using Conditional Logic (if/elif/else)
          |            |--- Mode 1 -> Set target_wins = 0 (Endless)
          |            |--- Mode 2 -> Set target_wins = 2 (Best of 3)
          |            |--- Mode 3 -> Set target_wins = 3 (Best of 5)
          |
          +---> 2.3: Reset Match Tracker variables to 0
                       |--- player_score = 0
                       |--- computer_score = 0
                       |--- total_rounds = 0
          |
          +---> [3] ROUND EXECUTION SUB-PATTERN (Inner Loop - Repeats Every Turn)
                  |
                  +---> 3.1: Data Collection & Cleaning
                  |            |--- Display current round string
                  |            |--- input() player choice -> apply .strip().lower()
                  |
                  +---> 3.2: Input Validation Check
                  |            |--- IF input is invalid -> trigger 'continue' -> loop back to 3.1
                  |            |--- ELSE -> pass barrier and proceed to 3.3
                  |
                  +---> 3.3: Opponent Processing (Call Helper Function)
                  |            |--- Run get_computer_choice() -> random.choice() picks a move
                  |
                  +---> 3.4: Evaluation & Referee Phase (Call Helper Function)
                  |            |--- Run determine_winner(player, computer)
                  |            |--- Return string outcome: 'win', 'lose', or 'tie'
                  |
                  +---> 3.5: Score Management Phase
                  |            |--- Increment total_rounds by 1
                  |            |--- Add +1 point to matching score tracker variable
                  |            |--- Output live score update to terminal screen
                  |
                  +---> 3.6: Match Boundary Check
                               |--- IF Target Wins reached (or user types 'n' in Endless)
                               |      |---> Trigger 'break' command -> Exit Round Sub-Pattern [3]
                               |--- ELSE
                                      |---> Circle back to 3.1 for next round
          |
          +---> [4] POST-MATCH SUMMARY PATTERN (Runs when Match Breaks)
                  |
                  +---> 4.1: Compute statistics via calculate_win_percentage() helper
                  |            |--- Apply zero-division safety guard check
                  |            |--- Format decimal value via round()
                  |
                  +---> 4.2: Output finalized summary scoreboard graphics block
                  |
                  +---> 4.3: Match Cycle Evaluation
                               |--- Prompt user: "Play a brand new match? (yes/no)"
                               |--- IF 'yes' -> loop back to Pattern [2] (scores reset)
                               |--- IF 'no'  -> trigger final 'break' -> Exit Match Pattern [2]
  |
  v
[FINISH / EXIT PROGRAM]